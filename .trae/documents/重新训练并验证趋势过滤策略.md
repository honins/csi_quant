# 重新验证“趋势过滤”策略 (Trend Only)

用户希望再次验证当前 `feat/trend_only` 分支（仅在趋势明确时预测）的效果，以确认其表现是否真的不如预期。

## 计划步骤

1.  **确认环境**
    -   检查当前 Git 分支，确保位于 `feat/trend_only`。
    -   检查 `src/prediction/prediction_utils.py`，确认趋势过滤逻辑（Trend Filter）已启用。

2.  **重新训练 AI 模型**
    -   运行 `python run.py ai`。
    -   **关键点**：AI 训练过程中的“贝叶斯优化”阶段会调用策略回测。由于我们修改了 `prediction_utils.py` 加入了趋势过滤，优化器会尝试寻找一组新的参数（如置信度阈值），使其在有趋势过滤的情况下仍能得分。这比单纯使用旧模型更公平。

3.  **执行全量回测**
    -   运行 1 年期滚动回测：`python run.py backtest 2025-01-11 2026-01-11`。
    -   （可选）运行 6 个月回测作为辅助对比。

4.  **结果对比与分析**
    -   对比本次重训后的结果与之前的 48% 准确率。
    -   确认该策略是否确实因过于保守而导致长期失效，或者通过参数自适应能有所改善。
